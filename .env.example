# Environment Variables for Netlify/Vercel Deployment
# Copy to .env.local (or set in hosting dashboard) for local development

# Admin password for login
ADMIN_PASSWORD=admin2026

# Admin key for retrieving orders (optional, for get-orders endpoint)
ADMIN_KEY=admin_orders_2026

# Orders file location (Netlify uses /tmp, Vercel uses /tmp)
# For persistent storage, use a database connection string instead
ORDERS_FILE=/tmp/orders.json

# Optional: Database connection (not used in serverless version, but kept for reference)
# DB_HOST=
# DB_USER=
# DB_PASS=
# DB_NAME=

# API Base URL (usually not needed on Netlify/Vercel, auto-uses /.netlify/functions or /api)
# API_BASE=https://your-site.netlify.app
